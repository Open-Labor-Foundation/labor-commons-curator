{
  "scripts": {
    "build": "npm run generate:types",
    "generate:types": "ts-node build/generate-types.ts"
  },
  "devDependencies": {
    "@types/json-schema": "^7.0.15",
    "json-schema-to-typescript": "^4.3.0",
    "ts-node": "^10.9.1"
  }
}
