{
  "name": "project-scaffold",
  "version": "1.0.0",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "pull-schema": "node scripts/pull-schema.js",
    "generate-types": "node scripts/generate-types.js",
    "generate-schema": "npm run pull-schema && npm run generate-types",
    "prebuild": "npm run generate-schema",
    "build": "tsc",
    "pretest": "npm run generate-schema",
    "test": "jest",
    "certify-record": "tsx src/certification/certify-record-cli.ts",
    "backfill-sweep": "tsx src/certification/backfill-sweep-cli.ts",
    "check:submodule-drift": "node scripts/check-submodule-drift.js"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^20.11.0",
    "@types/pg": "^8.20.0",
    "jest": "^30.4.2",
    "json-schema-to-typescript": "^15.0.4",
    "pg": "^8.22.0",
    "pg-mem": "^3.0.14",
    "ts-jest": "^29.4.12",
    "tsx": "^4.23.1",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "yaml": "^2.9.0"
  }
}
