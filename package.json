{
  "name": "schema-driven-project",
  "version": "1.0.0",
  "scripts": {
    "build": "./build-schema.sh && tsc",
    "postinstall": "npm run build"
  },
  "devDependencies": {
    "json2ts": "^1.0.0",
    "typescript": "^5.3.3"
  }
}