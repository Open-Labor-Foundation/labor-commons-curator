node_modules/
dist/
*.tsbuildinfo
.env
.env.local
coverage/

# Pulled fresh from labor-commons and regenerated on every `npm run build` /
# `npm test` (see scripts/pull-schema.js, scripts/generate-types.js) --
# never commit these.
src/schema/specialist-record.schema.json
src/schema/specialist-record.ts
